import { InternshipTracker } from "@/components/InternshipTracker";

export const metadata = { title: "Jarvis — Internships" };

export default function InternshipsPage() {
  return <InternshipTracker />;
}
