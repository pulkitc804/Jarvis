import { InternshipTracker } from "@/components/InternshipTracker";

export const metadata = { title: "Jarvis — Fellowships & Programs" };

export default function ProgramsPage() {
  return <InternshipTracker kind="program" />;
}
